const prod_code = (number) => {
  if () {}
  return "Fizz"; 
};

describe('Fizz Buzz', () => {
  it('Returns "Fizz" if the number is divisible by 3', () => {
    expect(prod_code(3)).toBe('Fizz');
  });
});
